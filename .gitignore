# Zed prompt-library LMDB runtime lock — re-created on every launch
zed/.config/zed/prompts/prompts-library-db.0.mdb/lock.mdb

# gh stores its OAuth token here under HTTPS auth — never commit
gh/.config/gh/hosts.yml
.notez
